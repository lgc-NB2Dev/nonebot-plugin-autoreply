- matches:
    - match: ^(在吗?|zai(\s*ma)?)\s*(？|\?)?$
      type: regex
      to_me: true

  replies:
    - - type: at
        data:
          qq: '{user_id}'
      - type: text
        data:
          text: Bot 在哦~
